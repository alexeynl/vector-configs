sources:
  src-test:
    type: vector
    address: 127.0.0.1:10001
transforms:
  tr-test:
    type: remap
    inputs:
      - src-test
    file: "${VECTOR_CONFIG_PATH}/main/vrl/test.vrl"
sinks:
  snk-test:
    type: blackhole
    inputs: 
      - tr-test